[build]
  command = "VITE_API_URL=https://vividya.onrender.com npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Redirect rules for React Router Single Page App routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
